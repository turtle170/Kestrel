[package]
name = "kestrel-init"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
nix = { version = "0.27", features = ["sched", "mount", "process", "fs"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
